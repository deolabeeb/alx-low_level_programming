#include "main.h"

/**
 * main - Entry point
 *
 * print_alphabet - prints the alphabet, in lowercase, followed by a new line.
 *
 * Return: Always 0
 */
int main(void)
{
	print_alphabet(void)
	{
		int r;

		for (r = 'a'; r <= 'z'; r++)
		{
			_putchar(r);
		}
		_putchar(\'n')
	}
	return (0);
}
