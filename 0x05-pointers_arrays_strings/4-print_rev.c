#include "main.h"

/**
 * print_rev - prints string in reverse followed by a new line
 *
 * @s: pointer to string
 *
 * Return: void
 */
void print_rev(char *s)
{
	int n;

	n = strlen(s);
	for (i = n - 1; i >= 0
	_putchar ('\n');
}
